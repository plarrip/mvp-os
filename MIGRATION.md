# v0.4 migration

Previous versions over-coupled MVP-OS to Spec Kit.

v0.4 restores the requirement that MVP-OS works without an SDD framework.

Spec Kit is an optional first-class integration. Other SDDs can be added later without changing the Lean-MVP core.
